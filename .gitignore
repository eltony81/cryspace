/docs/
/lib/
/bin/
/.shards/
*.dwarf

# Libraries don't need dependency lock
# Dependencies will be locked in applications that use them
/shard.lock
examples/*.csv

# Local development overrides (point shards at sibling checkouts)
/shard.override.yml
